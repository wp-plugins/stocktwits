=== StockTwits Widget ===
Contributors: stocktwits, Chris Corriveau, Gleb Esman
Tags: stocks, finance, investment, links, stocktwits, twitter
Requires at least: 2.2
Tested up to: 2.9
Stable tag: 1.7

The StockTwits plugin shows the most recent posts from www.StockTwits.com website as a widget. Choose to show your stock messages, just the Ticker stream, or the All stream. Show the messages in a post, in a theme, or as a widget.

== Description ==

The StockTwits plugin shows the most recent posts from www.StockTwits.com website as a widget. Choose to show your stock messages, just the Ticker stream, or the All stream. Show the messages in a post, in a theme, or as a widget.

It's pretty simple to run, just drop it in the plugin directory and activate. Then got to the Settings and configure the default plugin settings.

Then add it as a widget, theme code, or simple code method to your blog.

== Installation ==

1. Upload plugin files to the `/wp-content/plugins/` directory into it's own 'stocktwits' directory
2. Activate the plugin through the 'Plugins' menu in WordPress
3. Adjust the plugin settings through the 'Settings' menu. Choose the widget Title, type of stream (All, Ueername, Ticker), auto-refresh rate.
4. Insert plugin into a theme using the php code snippet or use the simple tag within a post or use a sidebar widget.

== Screenshots ==

1. This is the default look of the plugin in your sidebar. It uses an auto width and height
